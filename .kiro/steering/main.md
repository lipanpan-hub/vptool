---
inclusion: always
---

## 项目概述

这是一个基于 **OCLIF v4** 框架构建的 CLI 工具。项目使用 TypeScript（ESM 模块），编译输出到 `dist/`，通过 `bin/run.js` 作为可执行入口。


## 目录结构

项目根目录下的 src/commands 文件夹用来放置CLI命令参数相关代码
项目根目录下的 src/lib 文件夹用来放置命令核心逻辑函数
